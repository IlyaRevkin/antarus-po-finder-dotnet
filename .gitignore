## .NET
bin/
obj/
publish/
publish_test/
release_out/
*.user
*.pdb

## Installer build output (built from installer/Package.wxs — never the source)
installer/*.msi
installer/*.exe
installer/*.wixpdb
installer/*.log
.wix/

## Local runtime data (never ship a real firmware database)
*.db
*.db-shm
*.db-wal

## Test/automation scratch (screenshots, throwaway scripts — never app code)
AntarusPoFinder.Tests/scratch/
## Собранная копия для живого прогона GUI и её вывод — не исходники.
## Шаблон, а не точное имя: прогонов бывает несколько подряд (.livetest2, .livetest3_data,
## .livetest3_drive.py — сборка, изолированный профиль, скрипты pywinauto и скриншоты).
.livetest*
.testout/
.buildcheck/
.commitmsg.txt
## Стенд живого прогона GUI (pywinauto) и его изолированные профили — тоже черновик, не исходники.
scratchpad/

## IDE
.vs/
.vscode/
*.suo

## OS
Thumbs.db
.DS_Store

installer/*.sha256

## Loader creates this local SMLogix copy on demand; it is not a source or installer payload.
/installer/loader/SMLogix isolated payload/

## Живой GUI-прогон этого раунда: изолированный профиль, тестовый диск, скрипты pywinauto,
## скриншоты и собранный для прогона портатив — рядом с репозиторием, но не его часть.
.live/
.publish-live/

## Docs junk
docs/screenshots/
docs/*.pdf
*.psl

## Рабочие копии фоновых агентов — временные, не часть репозитория.
.claude/
