<UserControl x:Class="AntarusPoFinder.App.Views.OnboardingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="RootGrid" Background="#8A000000">
        <Border x:Name="HighlightBorder" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="3"
                CornerRadius="6" HorizontalAlignment="Left" VerticalAlignment="Top" IsHitTestVisible="False"/>

        <Border x:Name="CalloutBorder" Background="{DynamicResource BgSidebarBrush}" CornerRadius="8"
                Padding="16" Width="330" HorizontalAlignment="Left" VerticalAlignment="Top"
                BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1">
            <StackPanel>
                <TextBlock x:Name="StepCounterText" Style="{StaticResource MutedText}" Margin="0,0,0,6"/>
                <TextBlock x:Name="TitleText" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,6" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextBrush}"/>
                <TextBlock x:Name="BodyText" TextWrapping="Wrap" Margin="0,0,0,12"
                           Foreground="{DynamicResource TextBrush}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Пропустить всё" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Skip_Click"/>
                    <Button x:Name="BackButton" Content="Назад" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Back_Click"/>
                    <Button x:Name="NextButton" Content="Далее" Click="Next_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
