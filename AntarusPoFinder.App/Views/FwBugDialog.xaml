<Window x:Class="AntarusPoFinder.App.Views.FwBugDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="pack://application:,,,/AntarusPoFinder.App;component/Assets/icon.ico"
        Title="Баг в прошивке" Width="640" Height="520" MinWidth="480" MinHeight="420"
        Background="{DynamicResource BgBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource CardBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Прошивка" Style="{StaticResource MutedText}" FontSize="11"/>
                <TextBlock x:Name="FwLabelText" TextWrapping="Wrap" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="1" Margin="0,0,0,6"
                   Text="Насколько всё плохо"/>

        <StackPanel Grid.Row="2" x:Name="SeverityPanel" Orientation="Horizontal" Margin="0,0,0,12"/>

        <TextBox Grid.Row="3" x:Name="BodyInput" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                 VerticalContentAlignment="Top" Padding="8"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}" IsCancel="True" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Сообщить о баге" Style="{StaticResource PrimaryButton}" Click="Create_Click"/>
        </StackPanel>
    </Grid>
</Window>
